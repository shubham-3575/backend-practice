<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <link rel="stylesheet" href="/stylesheets/style.css">
    <script src="https://cdn.tailwindcss.com"></script>

</head>
<body>
    <div class="w-full h-full bg-zinc-900 text-white p-10">
        <a class="text-blue-500" href="/" >Back to home </a>
        <h1 class="px-3 py-2 mb-2 text-3xl mt-5 tracking-tighter">All Users</h1>
        <div class="users flex gap-3 flex-wrap">
            <% if(users.length>0) { %>
            <% users.forEach(function(user){ %>
                <div class="user w-72 p-4 bg-zinc-800 rounded-lg">
                    <div class="w-full h-52 rounded-lg bg-zinc-700">
                        <img class="w-full h-72 rounded-lg bg-zinc-700 overflow-hidden object-top" src="<%= user.image %>" alt="">
                    </div>
                    <h3 class="text-2xl tracking-tighter"><%= user.name %></h3>
                    <h5 class="text-zinc-500 "><%= user.email %></h5>
                    <div class="flex gap-3">
                        <a class="ml-2 text-zinc-200" href="/edit/<%= user._id %>">Edit user</a>
                        <a class="text-red-500 ml-20" href="/delete/<%= user.id %>">Delete User</a>
                    </div>
                </div>
            <% }) %>
        <% } else { %>
           <h3>No users yet</h3> 
        <% } %>
         </div>
    </div>
</body>
</html>
